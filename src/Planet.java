/**
 * a planet type of astronomical body
 */
public class Planet extends AstronomicalBody {
    /**
     * construct a planet with given name and mass
     * @param mass the given mass
     * @param name the given name
     */
    public Planet(double mass, String name) {
        super(mass, name);
    }
}
